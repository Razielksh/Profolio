meta {
  name: 04d - Cambiar Estado Activo/Inactivo
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/api/admin/users/1/toggle-status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

tests {
  test("Status es 200 OK", function() {
    expect(res.status).to.equal(200);
  });
}
